# State ledger — horn-10ghz

Three phase sessions bound by this file (ADR 0007): Clarification → Build
(through the Review gate incl. read-back sync) → Solve+QA. Each starts here,
not from the prior conversation. Machine state lives in `results/state/*.txt` —
never hand-edited.

## Session 1 — Clarification (locked in the UI, never changed after)

- Started: 2026-08-16T20:30:00Z
- Recipe: pyramidal-horn (Balanis ch. 13 optimum synthesis; playbook
  precheck estimator `horn_cutoff`, tolerance 10%)
- Locked parameters / variables (all in `design.yaml`):
  - f0 = 10 GHz; sweep 8–12 GHz interpolating, 201 pts
  - WR-90 feed: wg_a = 0.9in, wg_b = 0.4in, wall = 0.05in
    (TE10 cutoff 6.557 GHz; f/fc = 1.525 — single-mode band OK)
  - Aperture: horn_a = 3.045in, horn_b = 2.25in (synthesis output)
  - flare_len = 1.501in (pe = ph; both flares meet the feed at one station)
  - Expected gain: 14.99 dBi (eap 0.51) vs 15 dBi target
  - Airbox pad 0.4in (~λ0/3) all sides
- Assumptions (all confirmed by the user):
  1. WR-90 = standard inner 0.900 x 0.400 in, standard wall 0.050 in.
  2. "15 dBi" = target for the closed-form; ±10% playbook tolerance;
     the optimum synthesis closes on 14.99 dBi.
  3. Port = wave port on the throat opening at the port plane z=0
     (tutorial/ATK topology; sheet-name assignment, the compiler's proven path).
  4. Modal solution type, PEC conductor, adaptive-only mesh, radiation airbox.
  5. Result readout: S11/gain read via the AEDT UI on this box is
     authoritative; scripted readout is a bonus (never a blocker).
- Approved Result QA signals: convergence, ports_excited, gain_plausible.
- Route: A (Design Spec). Construction note: the two `connect` lofts are
  terminal objects (nothing references them by name afterwards) so the model
  does not depend on HFSS's loft result-naming; watch item for the Build
  session — confirm the lofted solids and the fate of the profile sheets in
  the captured snapshot.

## Session 2 — Build

Route A: `compile_spec.py` walks the Spine stages. Ledger deltas per stage
land here after each compile Run; the offline gates (validate_spec, precheck,
compile_spec --dry-run) are green from Clarification (see below).

- validate_spec: PASS: validate_spec errors=0 warnings=0
- precheck: PASS: precheck recipe=pyramidal-horn verdict=consistent
  (closed-form 14.986 dBi vs 15.0 target, delta -0.09%, tolerance 10%;
  TE10 gate f/fc = 1.5251, single-mode OK)
- compile_spec --dry-run: PASS: plan 9 stages, 8 geometry ops, 1 port(s),
  1 boundary(ies), escape_hatch=0
- tier0 pre-flight: 7/10 suites pass; the 3 failures are pre-existing
  worktree conditions, not this spec (horn-10ghz case dir moved aside for
  S3; pilot snapshot lives in a gitignored workspace; skill junction path)
- compile_spec --launch: <paste PASS line>
- capture_state / spec_acceptance: <paste PASS line>

## Session 3 — Solve + QA

- Watchdog: `results/state/solve_progress.txt` (running | settling |
  complete | stalled — the agent reads only)
- QA signals: <numbers per agreed signal, or "unreadable — flaky readout">
- Run card: appended to `summary.md` by `scripts/run_card.py`

## Pointers

- Spec: `workspaces/horn-10ghz/design.yaml` (Clarification's named artifact)
- Model snapshot: `results/state/model_snapshot.json` (`capture_state.py`;
  replayed + diffed by `spec_acceptance.py`)
- Machine state: `results/state/*.txt` (`aedt_port`, `aedt_process_id`,
  `solve_progress`, …)
- Reference case: `knowledge/cases/horn-10ghz` (canonical spec moved aside
  for the S3 blind rebuild; snapshot fixture at
  `knowledge/cases/_snapshots/horn-10ghz.json` is the ATK tutorial model, NOT
  this design — different aperture/length — do not match it)
