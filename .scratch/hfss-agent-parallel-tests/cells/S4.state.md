# State ledger — dipole-2450

Three phase sessions bound by this file (ADR 0007): Clarification → Build
(through the Review gate incl. read-back sync) → Solve+QA. Each starts here,
not from the prior conversation. Keep ≤ ~2 KB: stage progress, locked
parameters, pitfalls, pointers. Machine state lives in `results/state/*.txt` —
never hand-edited; the staged scripts write it.

## Session 1 — Clarification (locked in the UI, never changed after)

- Started: 2026-08-16T21:15:53Z
- Recipe: `half-wave-dipole` — **NEW, derived in this Clarification** (playbook has
  no dipole entry; precheck has no estimator → verdict `no-estimator`, never blocks).
  Learning-loop candidate: estimator + tolerance + worked example for the playbook.
- Assumptions (user confirmed 2026-08-16):
  - Free space = airbox + radiation boundary, λ0/4 = 30.6 mm clearance; no ground,
    no balun.
  - Arms collinear along +Z, centered at origin (cylinders on the axis).
  - Wire = PEC (user asked copper; 1 mm copper skin depth ≈ 1.4 µm @ 2.45 GHz,
    PEC is exact for this purpose).
  - Feed gap 2.0 mm; lumped port (50 Ω, renormalized) on a ribbon sheet in the gap
    (XZ plane), integration line along the axis from −Gap/2 to +Gap/2.
  - Resonance target 2.45 GHz ±5 %: L_tot = 0.474·λ0 (468/f rule, Balanis). L_arm is
    a variable so a post-solve tune is a variable edit, not a rebuild.
  - Expected: Z_in ≈ 65–72 Ω + j0 at resonance; S11 min ≈ −15 ± 1 dB at 50 Ω
    (bare half-wave dipole is ~1.4:1 SWR — NOT a matched design by construction).
- Approved Result QA signals: convergence, ports_excited, in_band_resonance
  (2.45 ±5 %), energy_pass — plus ledger-level: Z_in plausible (R 60–75 Ω, X crosses 0),
  S11 min −14.5 ± 2 dB.
- Locked variables: `f0 = 2.45GHz`, `L_arm = 28mm`, `Gap = 2mm`, `WireD = 1mm`,
  `PortW = 4mm`, `AirGap = 30.6mm`, `L_tot = 2*L_arm + Gap`.
- Sweep locked at **1.9–3.0 GHz, 221 pts** (widened from 2.0 GHz after the
  validator warned the near-side margin was thin — gap capacitance could pull
  resonance down; 0.55 GHz of margin now).
- Build route: **A** (design spec + compiler). Offline gates 2026-08-16:
  validate_spec `errors=0 warnings=0`; precheck `no-estimator` (honest — no
  dipole estimator in playbook yet, learning-loop candidate); compile
  `--dry-run` PASS (9 stages, 4 geometry ops, 1 port, 1 boundary,
  escape_hatch=0).
- Note: tier0 reports 2 pre-existing suite failures (skill-install,
  design-spec) — both are `workspaces/bowtie-3500-pilot/.../model_snapshot.json`
  missing (gitignored pilot workspace, not carried into this worktree).
  Environmental, unrelated to this workspace.

## Session 2 — Build

One stage = one script = one Run, each ending in its `PASS:` Verification line:

| Stage | Script | Verification line |
|-------|--------|-------------------|
| Solution type + design | (compiler route A) | <paste PASS line> |
| Geometry | (compiler route A) | <paste PASS line> |
| Materials | (compiler route A) | <paste PASS line> |
| Excitations / boundaries | (compiler route A) | <paste PASS line> |
| Mesh | (compiler route A) | <paste PASS line> |
| Setup + sweep | (compiler route A) | <paste PASS line> |
| Validation | (compiler route A) | <paste PASS line> |
| Review gate + sync verify | `capture_state.py` + `spec_acceptance.py` | <paste PASS line> |

- Pitfalls hit: <one line each: what, why, which script it amends>

## Session 3 — Solve + QA

- Watchdog: `results/state/solve_progress.txt` (running | settling |
  complete | stalled — the agent reads only)
- QA signals: <numbers per agreed signal, or "unreadable — flaky readout">
- Run card: appended to `summary.md` by `scripts/run_card.py`

## Pointers

- Model snapshot: `results/state/model_snapshot.json` (`capture_state.py`;
  replayed + diffed by `spec_acceptance.py` vs `as_built.json`)
- Machine state: `results/state/*.txt` (`aedt_port`, `aedt_process_id`,
  `solve_progress`, …)
- Preconditions for any AEDT launch: UM VPN + license (1055@LICENSE-ANSYS…).
