# State ledger — patch-array-5800

Three phase sessions bound by this file (ADR 0007): Clarification → Build
(through the Review gate incl. read-back sync) → Solve+QA. Each starts here,
not from the prior conversation. Machine state lives in `results/state/*.txt` —
never hand-edited; the staged scripts write it.

## Session 1 — Clarification (locked in the UI, never changed after)

- Started: 2026-08-16T21:17:25Z
- Recipe: **corporate-patch-array** (derived in this Clarification; registered
  in `knowledge/playbook/precheck-tolerances.json` with the existing
  `patch_resonance` estimator — playbook amendment approved with the design)
- Assumptions (user confirmed "use your own recommended value" on all choices):
  - RO4350B: er = 3.48 (Rogers design DK), tanδ = 0.0037, h = 0.762 mm,
    copper t = 0.035 mm (1 oz); metal modeled as zero-thickness PEC sheets
  - Elements inset-matched to **100 Ω** (corporate scheme fits S = λ0/2;
    50 Ω-element transformers would not fit the 12.2 mm corridor)
  - Spacing **S = λ0/2 = 25.8442 mm** exactly (broadside, no grating lobes)
  - Substrate 52×52 mm; air pad λ0/4 = 12.92 mm; wave port (8 mm wide) at the
    input edge; radiation boundary on all airbox outer faces
  - Inset depth is the match tuner (start 2.0 mm, gap 1.0 mm), NOT the
    resonance — resonance is set by patch_L
  - Expected: S11 min at 5.8 ± 0.1 GHz < −10 dB; broadside gain 10.5–12.5 dBi;
    bandwidth ~1–2% (thin substrate, h/λ0 ≈ 1.5%)
  - Readout on this box is flaky (env-compat #6): S11/gain read via the AEDT
    UI as authoritative; scripted readout is a bonus
- Approved Result QA signals: convergence, ports_excited,
  in_band_resonance, energy_pass, gain_plausible

Locked parameters / variables (also `design.yaml`, the single source):

| variable | value | source |
|---|---|---|
| f0 | 5.8 GHz | user |
| er / tand / h / cu_t | 3.48 / 0.0037 / 0.762 mm / 0.035 mm | RO4350B datasheet (assumption) |
| patch_W / patch_L | 17.2679 mm / 13.6238 mm | Balanis 14-6 / 14-7 (recomputed, closes at 5.8000 GHz) |
| S | 25.8442 mm | λ0/2 |
| feed50_W / feed70_W / feed100_W | 1.7427 / 0.9464 / 0.4403 mm | Hammerstad synthesis on this stack |
| q70 | 7.984 mm | λg/4 at 70.7 Ω |
| inset_d / inset_g | 2.0 mm / 1.0 mm | match tuner — expect to adjust in build |
| sub_W / sub_L | 52 / 52 mm | patch extents + margin |
| air_pad / port_w | 12.9221 mm / 8 mm | λ0/4 / ~4.6·W50 |

Geometry notes for Build: all four patches + feed network unite into ONE
conductor that keeps the first operand's name (**P1**) — schema unite keeps the
first object's name; the 8 notch sheets are then subtracted from it. Airbox −y
face sits flush with the substrate edge (port plane), port pattern identical to
the proven `microstrip-50r` case.

## Session 2 — Build

One stage = one script = one Run, each ending in its `PASS:` Verification line:

| Stage | Script | Verification line |
|-------|--------|-------------------|
| Solution type + design | `01_solution_type_and_design.py` | |
| Geometry | `02_geometry.py` | |
| Materials | `03_materials.py` | |
| Excitations / boundaries | `04_excitations.py` | |
| Mesh | `05_mesh.py` | |
| Setup + sweep | `06_setup_sweep.py` | |
| Validation | `07_validate.py` | |
| Review gate + sync verify | `capture_state.py` + `12_verify_sync.py` | |

- Route A: `workspaces/patch-array-5800/design.yaml` → offline gates
  (validate_spec → precheck → compile dry-run, all PASS 2026-08-16) →
  `compile_spec.py --launch` in the Build session
- Pitfalls to watch: inset depth tuning (S11 depth, not position); T-junction
  parasitics at T1/T2 (miter if S11 shows a split/null); united-conductor
  naming quirk (object "P1")

## Session 3 — Solve + QA

- Watchdog: `results/state/solve_progress.txt` (running | settling |
  complete | stalled — the agent reads only)
- QA signals: numbers per agreed signal, or "unreadable — flaky readout"
- Run card: appended to `summary.md` by `scripts/run_card.py`

## Pointers

- Model snapshot: `results/state/model_snapshot.json` (`capture_state.py`;
  replayed + diffed by `12_verify_sync.py`)
- Machine state: `results/state/*.txt` (`aedt_port`, `aedt_process_id`,
  `solve_progress`, …)
