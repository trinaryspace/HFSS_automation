{
  "$schema": "https://opencode.ai/config.json",
  "model": "fireworks-ai/accounts/fireworks/models/deepseek-v4-flash-0731",
  "compaction": {
    "auto": true,
    "prune": true,
    // reserved: token buffer left in the window so compaction auto-trigger
    // never overflows mid-compaction on the 1M-context flash model.
    "reserved": 10000
  },
  // HFSS agent tiering (perf-refactor tickets 02/08). The subagent model is a
  // single swap point: both kb-lookup and runcard reference
  // `fireworks-ai/hfss-subagent`. Documented default: deepseek-v4-flash-0731
  // @ variant low (reasoningEffort=low, verified via `opencode models`).
  // Downgrade path: point the alias `id` at a cheaper model (e.g.
  // accounts/fireworks/models/gpt-oss-20b) and adjust the `variants` map —
  // both agents follow with no edits to the agent definitions.
  "provider": {
    "fireworks-ai": {
      "models": {
        "hfss-subagent": {
          "id": "accounts/fireworks/models/deepseek-v4-flash-0731",
          "name": "HFSS subagent model — single swap point (ticket 08)",
          "variants": {
            "low": {
              "reasoningEffort": "low"
            }
          }
        }
      }
    }
  },
  // Re-pilot 2 (2026-08-16): pinned at `max` to MATCH the kind-rocket run
  // (346,993 billed / 477 parts, patch-2400, free-form route) so the Design
  // Spec route is the only variable that moved. Revert to `low` after the run.
  // Prior context — ticket 06 ran at `max` and was reverted to `low`. Result: patch-2400
  // completed at 346,993 billed / 477 parts all-in (269,378 / 412 main
  // loop) against the 398,130 baseline — the first completed run since the
  // refactor, but well short of the >=50% bar. `max` was confirmed live via
  // `opencode debug config`; the DB's `tokens_reasoning: 0` is a provider
  // reporting gap on this model, not a sign the variant was inactive.
  // See .scratch/hfss-agent-spec-driven/issues/06-main-loop-model-experiment.md.
  "agent": {
    "build": {
      "variant": "max"
    },
    "kb-lookup": {
      "description": "Read-only pyAEDT API lookup against the local KB; returns exact signatures quoted from KB files or NOT FOUND.",
      "mode": "subagent",
      "model": "fireworks-ai/hfss-subagent",
      "variant": "low",
      "permission": {
        "read": "allow",
        "glob": "allow",
        "grep": "allow",
        "list": "allow",
        "edit": "deny",
        "bash": "ask",
        "task": "deny"
      },
      "prompt": "You answer how to call pyAEDT APIs. Read the local KB under `scraping/pyaedt_ai_context/`, return the exact signature and argument names by quoting the KB file (include the file path). If a call is not in the KB, reply exactly `NOT FOUND — <what you searched>`. Never paraphrase from memory. Keep answers to the signature + the one example."
    },
    "runcard": {
      "description": "Drafts summary.md sections and the run card from the workspace state.md and results for the main loop to revise.",
      "mode": "subagent",
      "model": "fireworks-ai/hfss-subagent",
      "variant": "low",
      "permission": {
        "read": "allow",
        "glob": "allow",
        "grep": "allow",
        "list": "allow",
        "edit": {
          "*": "deny",
          "workspaces/**": "allow"
        },
        "bash": "deny",
        "task": "deny"
      },
      "prompt": "Draft `summary.md` sections (What the Model is, Acute design decisions, QA signals results) and the `## Run card` from the workspace's `state.md`, `results/state/*.txt` and `results/` — concise, ≤250 words total. When `run-report.json` exists beside `summary.md`, narrate from it: take every number (outcome, completions, billed, wall, tokens by phase, findings) from its `headline` and `findings` exactly as written, name the top findings by kind and cost, and keep any value it marks `unmeasurable` or `unrecorded` as that — you never compute, sum, or estimate a number yourself. Flag anything unreadable instead of guessing."
    }
  }
}
